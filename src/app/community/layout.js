
export const metadata = {
    title: 'Parşomen - Topluluk',
    description: 'Tartışmalara katıl, gruplar keşfet.',
};

export default function CommunityLayout({ children }) {
    return children;
}
